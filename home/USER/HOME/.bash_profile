#
# ~/.bash_profile
#

# https://superuser.com/questions/789448/choosing-between-bashrc-profile-bash-profile-etc
[[ -f ~/.profile ]] && . ~/.profile
[[ -f ~/.bashrc ]] && . ~/.bashrc
[[ -f ~/.env ]] && . ~/.env
